<tbody id="projects-list">
  <% if projects.any? %>
    <% projects.each do |project| %>
      <%= card_for project, size: :voting_list_item, voting_token: vote_card.voting_token, vote_card: vote_card, project: project, step: params[:step] %>
    <% end %>
  <% end %>
</tbody> 