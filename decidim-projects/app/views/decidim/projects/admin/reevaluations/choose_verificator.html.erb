<div class="card">
  <div class="card-divider">
    <h2 class="card-title">
      <%= link_to "Projekty > ", projects_path %>
      <%= decidim_html_escape(@project&.title).html_safe %>
    </h2>
  </div>

  <div class="card-section">
    <div class="card">
      <div class="card-divider">
        <h2 class="card-title">
          Wybierz weryfikatora by przekazać projekt do ponownej oceny
        </h2>
      </div>
      <div class="card-section">
        <%= decidim_form_for(@form, url: submit_for_verification_project_reevaluations_path(@project), method: :post, html: { class: "form new_user" }) do |f| %>
          <%= f.select :evaluator_id, @form.possible_verifacator_ids, include_blank: true, label: 'Weryfikator' %>

          <div class="button--double form-general-submit">
            <%= f.submit 'Przypisz' %>
          </div>
        <% end %>

      </div>
    </div>
  </div>
</div>
